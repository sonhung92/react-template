import styled from 'styled-components';
import { Modal } from 'semantic-ui-react';

const StyledContentModal = styled(Modal.Content)`
  &&& {
    /* your css */
  }
`;

export default StyledContentModal;
